import { Notifications } from "@/screens/Notifications";

// Hidden tab (href:null) — reached from Home / Account. Available to every role.
export default function NotificationsTab() {
  return <Notifications />;
}
