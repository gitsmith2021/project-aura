# Aura Campus — Database Design

> **Status:** Living — schema is defined by migrations.

The authoritative Aura Campus schema is the set of SQL migrations in [`supabase/migrations/`](../../supabase/migrations/) (repo root). Naming conventions and the multi-tenant model are documented in [`AURA_ROADMAP.md`](AURA_ROADMAP.md) (System Context).

This document will hold an ER overview and table catalogue as the schema stabilises.
